%fieldset
  %legend Create A New Entry Here

  = form_for(entry, html: {id: 'new-entry'}) do |f|
    = f.label(:title, 'Title')
    = f.text_field(:title)

    = f.label(:content, 'Content')
    = f.text_area(:content)

    = f.hidden_field(:categories, value: current_tags.map(&:id).join(','))
    = f.submit(id: 'create-entry')
